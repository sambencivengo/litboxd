import * as Users from './users';
import * as Reviews from './reviews';
import * as ReadingList from './readingList';

const Controllers = {
	Users,
	Reviews,
	ReadingList,
};

export default Controllers;
